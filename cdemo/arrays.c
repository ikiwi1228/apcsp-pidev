#include <stdio.h>

int main()
{
	int arr[100];

	for (int i = 0; i = 100; i++)
	{
		arr[i] = i * i;
	}

	int arrayAdd(int arr[],int s,int n)
	{
		for (int i=0; i=int s; i++)
		{
			int arr[i]+=int n
		}
	}

	for (int i=0; i=100; i++)
		{
		printf("arr[%d],Arr[%d]",arr[i],arr[i])
		}
}
